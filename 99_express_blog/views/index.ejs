<!DOCTYPE html>
<html lang="ko">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>메인페이지</title>
    <!-- head 인클루드 (인클루드 시 확장자 생략 가능)-->
    <%-include('./include/head')%>
  </head>
  <body>
    <!-- header 인클루드 -->
    <%-include('./include/header.ejs')%>

    <!-- 
        넘어오는 데이터 형태
        {
            user: userID,
            contentData: tempDB, //[{}, {}]
        });
     -->
    <h2>안녕하세요! <%=user%>의 블로그입니다.</h2>
    <ul>
      <!-- 배열 안에서의 for문인 for of문 사용 -->
      <%for(let content of contentData){%>
      <li>
        <a href="/content/<%=content.contentID%>"><%=content.title%></a>
        <button onclick="deleteContent('<%=content.contentID%>', this)">
          삭제
        </button>
      </li>
      <%}%>
    </ul>
    <script>
      function deleteContent(contentID, btn) {
        // console.log(contentID);
        // console.log(btn); // this로 전달된 button 태그
        if (confirm("삭제하시겠습니까?")) {
          //삭제 로직 작성
          axios({
            method: "delete",
            url: "blog/delete?contentID=" + contentID,
          }).catch(function (err) {
            console.error(err);
          });

          // btn의 부모인 li 삭제
          //   console.log(btn.parentNode);
          btn.parentNode.remove();
        }
      }
    </script>
  </body>
</html>
